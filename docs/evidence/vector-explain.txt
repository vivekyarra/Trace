Phase 0 observed plan
Cluster: lucid-owlet (CockroachDB CCL v26.2.5)
Database: defaultdb

Statement:
EXPLAIN SELECT id, embedding <=> '[1,0,0]' AS cosine_distance
FROM lore_preflight_vectors
WHERE organization_id = '00000000-0000-0000-0000-000000000001'
  AND repository_id = '00000000-0000-0000-0000-000000000002'
ORDER BY embedding <=> '[1,0,0]'
LIMIT 3;

Observed output:
distribution: local
vectorized: true

• top-k
│ estimated row count: 3
│ order: +cosine_distance
│ k: 3
│
└── • render
    │
    └── • filter
        │ estimated row count: 3
        │ filter: (organization_id = '00000000-0000-0000-0000-000000000001')
        │   AND (repository_id = '00000000-0000-0000-0000-000000000002')
        │
        └── • scan
            │ estimated row count: 3 (100% of the table; stats collected 12 minutes ago)
            │ table: lore_preflight_vectors@lore_preflight_vectors_pkey
            │ spans: FULL SCAN

index recommendations: 1
1. type: index creation
   SQL command: CREATE INDEX ON defaultdb.public.lore_preflight_vectors
     (organization_id, repository_id) STORING (embedding);

Conclusion: the database accepted both the plain and prefixed vector indexes,
but this tiny filtered corpus plans a primary-key full scan. This is not proof
of vector-index acceleration.
